# RISE 課程分類與作業指派更新

本更新以 RISE 版本 5b140f9c0bec3c95c4307ddea59b097e78519866 為基礎。正式資料庫仍須由管理員執行下列 SQL。

## 安裝
1. 先備份目前網站。將本更新包的 HTML、JS 與 backend、tests 檔案按原路徑放回 RISE 儲存庫；不要覆蓋 auth-config.js（本包未包含）。如果 GitHub 上已有其他新修改，請先比對再合併。
2. 在已完成 RISE 帳號後端設定的 Supabase SQL Editor，執行完整的 `backend/course-assignments.sql`，不要只複製函式片段。此檔包含學習流程建表與課程升級，可重複執行。
3. 提交網站檔案，等 GitHub Pages 部署完成後測試。若之後重跑舊的 `learning-workflows.sql`，必須再執行此升級檔，避免舊版函式覆蓋指派權限。

## 使用方式
- 管理員專區 → 課程分類管理：新增、修改、停用課程。
- 學生註冊：選擇所屬課程，可略過、可複選（最多20門）。
- 學生會員中心 → 我的課程：更新選課。
- 作業與批閱管理 → 新增作業：指定課程、指定學生或全部學生。
- 開啟自己建立的作業 → 調整指派對象：修改對象。管理員可管理所有作業。
- 指定學生可搜尋姓名或完整會員編號；同名學生以會員編號區分。

## 權限與資料
課程為學生自行選擇，不是經審核的選課名單；若要嚴格限定學員，請使用「指定學生」。選課不會變更學生／助教／教師／管理員角色。
原有作業保留為全部學生可見。新作業預設指定課程，未選對象不能建立。課程作業適用於當下選課的學生（之後加入者也會看到）；退出後不能讀取或繳交該課作業，但繳交與批閱歷程仍保留供教學人員查看。
停用課程會停止新加入，既有學員與其作業可見性維持。此更新不更動教師／助教原有批閱範圍。

## 驗證
已在隔離 PostgreSQL 相容環境執行完整 SQL 兩次，測試課程管理權限、作業列表／讀取／繳交限制、指定學生、指派修改權限、退出課程與歷程保存、註冊自選課程與原有作業／批閱／培訓／競賽流程。
DOM 測試涵蓋四種角色與四種學習頁面、註冊與會員選課、指派表單、錯誤保留輸入、文字跳脫及管理員頁面限制。
未驗證正式 Supabase 與真實帳號的端到端流程；GitHub 提交不會自動執行資料庫 SQL。

測試（需 Node.js，先在 tests/workflows 安裝 package.json 依賴）：
- node tests/workflows/test-courses.mjs
- node tests/workflows/test-ui.mjs
- node tests/workflows/test-course-ui.mjs
